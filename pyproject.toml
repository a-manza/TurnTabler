[project]
name = "turntabler"
version = "0.1.0"
description = "Stream vinyl records to Sonos speakers with lossless audio quality"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "soco",
    "fastapi",
    "uvicorn[standard]",
]
